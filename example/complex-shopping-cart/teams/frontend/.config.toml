[iteration.status.map]
todo = { category = "todo", order = 10 }
planning = { category = "todo", order = 20 }
in_flight = { category = "in_progress", order = 30 }
review = { category = "in_progress", order = 40 }
done = { category = "done", order = 50 }
