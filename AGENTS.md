# AGENTS

# Repository status
- This repository defines a formal spec (see `SPEC.md`) and supporting docs.
- This repo contains specs, documentation, and examples only (no implementation code).
- No build, lint, or test tooling detected.
- No Cursor or Copilot instruction files found.

# How to update this file
- When code is added, revisit this file and replace the placeholders below.
- Prefer commands from existing config files (package.json, Makefile, pyproject.toml, etc.).
- Keep this file short, precise, and runnable by agents.

# Build commands
- Primary build: N/A (no build system detected).

# Lint commands
- Primary lint: N/A (no lint tooling detected).

# Test commands
- Primary test: N/A (no test tooling detected).

# Single test recipes (templates)
- N/A

# Code style guidelines (defaults until repo specifies otherwise)
# Documentation
- Update `SPEC.md` first for spec changes.
- Bump spec version per semantic versioning rules.

# Cursor/Copilot rules
- None detected in .cursor/rules/, .cursorrules, or .github/copilot-instructions.md.
